<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project.photoapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <style
            android:name="AppTheme"
            parent="@style/Theme.AppCompat.Light.NoActionBar">
            ...
            <item name="windowNoTitle">
                true
            </item>
            <item name="android:windowNoTitle">
                true
            </item>
        </style>
        <!-- FX Filter Activity -->
        <activity android:name=".MainPage" />
        <!-- Splash Screen Activity -->
        <activity android:name=".SplashScreen">
            <intent-filter>
                <!-- Main Page Activity -->
                <action android:name="android.intent.action.MAIN" />
                <!-- Launcher Activity -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Loader Page Activity -->
        <activity android:name=".Loader" />
        <!-- Recent Images Gallery Activity -->
        <activity android:name=".RecentImages" />
        <!-- Base Editor Activity -->
        <activity android:name=".BaseEditor" />
        <!-- Transform0 Tools Activity -->
        <activity android:name=".Transform0" />
        <!-- Adjust Tools Activity-->
        <activity android:name=".Adjust1" />
        <!-- Brush Tools Activity-->
        <activity android:name=".Brush2" />
        <!-- Overlay Tools Activity-->
        <activity android:name=".Overlay3">
        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>